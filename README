================================================================================
SGL - Software GL
================================================================================

SGL implements a subset of standard OpenGL APIs with pure software simulation
(without GPU)
